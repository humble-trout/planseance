# Docker maison (pas besoin pour les étudiants)

Run : docker-compose -f docker/docker-compose-postgre.yml up --build -d

Accès pgadmin : http://localhost:16543/browser/

Stop docker-compose -f docker/docker-compose-postgre.yml down 
